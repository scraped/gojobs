export default class HttpService {
  _httpInstance;

  constructor(httpInstance = axiosInstanceFactory.) {
    this._httpInstance = httpInstance;
  }

  static axiosInstanceFactory({

  })
}
